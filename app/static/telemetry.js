(()=>{const send=(event_type,metadata={})=>{fetch('/analytics/event',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({event_type,metadata}),keepalive:true}).catch(()=>{})};const selected=(name)=>Array.from(document.querySelectorAll(`input[name="${name}"]:checked`)).map(i=>i.value);document.getElementById('iniciar')?.addEventListener('click',()=>send('search',{hours:document.querySelector('input[name="horas"]:checked')?.value||'24',editorias:selected('editoria'),order:document.querySelector('input[name="ordenar"]:checked')?.value||'editor_chefe'}));document.getElementById('copiar')?.addEventListener('click',()=>send('copy_results',{visible_results:document.querySelectorAll('.news-card').length}));document.addEventListener('click',event=>{const target=event.target.closest('.copy-article,.result-filter-button');if(!target)return;if(target.classList.contains('copy-article')){const card=target.closest('.news-card');send('copy_article',{title:card?.querySelector('h2')?.textContent||''})}else{send('result_filter',{editorial:target.textContent.trim()})}})})();