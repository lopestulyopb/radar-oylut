services:
  - type: web
    name: radar-oylut
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main_v708:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /saude
