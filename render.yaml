services:
  - type: web
    name: radar-oylut
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main_v7:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /saude
